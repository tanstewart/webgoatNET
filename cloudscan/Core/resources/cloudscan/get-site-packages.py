import sys
for f in sys.path:
    print(f) # remove this line and replace with following two ones as soon as python core is a part of Sensor installation
#    if f.endswith('packages'):
#        print(f)
